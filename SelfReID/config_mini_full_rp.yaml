# lightning.pytorch==2.4.0
seed_everything: true
trainer:
  accelerator: auto
  strategy: auto
  devices: auto
  num_nodes: 1
  precision: null
  logger:
       class_path: TensorBoardLogger
       init_args:
          save_dir : ./outputs/MINI_FULL/SGD/BATCH_${data.batch_size}_1E4_RandomPerspective/${model.backbone}_dim${model.hidden_dims}_size${model.imsize}/logs/
          name: ${data.dataset_name}
          default_hp_metric: False
  callbacks: null
  fast_dev_run: false
  max_epochs: 80
  min_epochs: null
  max_steps: -1
  min_steps: null
  max_time: null
  limit_train_batches: null
  limit_val_batches: null
  limit_test_batches: null
  limit_predict_batches: null
  overfit_batches: 0.0
  val_check_interval: null
  check_val_every_n_epoch: 1
  num_sanity_val_steps: 0
  log_every_n_steps: null
  enable_checkpointing: null
  enable_progress_bar: null
  enable_model_summary: null
  accumulate_grad_batches: 1
  gradient_clip_val: null
  gradient_clip_algorithm: null
  deterministic: null
  benchmark: null
  inference_mode: true
  use_distributed_sampler: true
  profiler: null
  detect_anomaly: false
  barebones: false
  plugins: null
  sync_batchnorm: false
  reload_dataloaders_every_n_epochs: 1
  default_root_dir: null
model:
  backbone: ResNet18
  hidden_dims: 64
  lossname: NTXentLoss()
  lr: 0.0001
  imsize: 128
  mining: true
  augment: true
  save_path: ./outputs/MINI_FULL/SGD/BATCH_${data.batch_size}_1E4_RandomPerspective/${model.backbone}_dim${model.hidden_dims}_size${model.imsize}
data:
  dataset_name: MaskDataset
  root: ./data/MaskDataset/images/STD_masks_mini_full
  batch_size: 32
  num_workers: 16
optimizer:
  class_path: torch.optim.SGD
  # init_args:
    # betas:
    # - 0.9
    # - 0.999
    # eps: 1.0e-08
    # weight_decay: 0.01
    # amsgrad: false
    # maximize: false
    # foreach: null
    # capturable: false
lr_scheduler: null
  # class_path: torch.optim.lr_scheduler.ReduceLROnPlateau
  # init_args:
  #   mode: min
  #   factor: 0.1
  #   patience: 10
  #   threshold: 0.0001
  #   threshold_mode: rel
  #   cooldown: 0
  #   min_lr: 0
  #   eps: 1.0e-08
  #   verbose: deprecated
  # monitor: train_loss
ckpt_path: null
